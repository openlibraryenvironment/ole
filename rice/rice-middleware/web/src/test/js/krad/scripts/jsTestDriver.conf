# should be the same port as the one used to start the server
server: http://localhost:9876

# the paths below assume the tests will be run with a --basePath flag that points to web/src
load:
  - main/webapp/krad/scripts/krad.utility.js
  - main/webapp/plugins/jquery/jquery-1.8.0.js
  - test/js/krad/scripts/*.js